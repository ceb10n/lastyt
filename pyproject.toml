[project]
name = "lastyt"
version = "0.1.0"
description = "Sync YouTube Music listening history to Last.fm"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ytmusicapi>=1.12.0",
    "pylast>=5.3",
    "httpx>=0.27",
]
